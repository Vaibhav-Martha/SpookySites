{
  "name": "Test and Format Code",
  "description": "Runs tests and formats code when files are saved",
  "trigger": {
    "type": "file_save",
    "pattern": "**/*.{js,jsx,ts,tsx}"
  },
  "actions": [
    {
      "type": "message",
      "content": "File saved! Running tests and formatting code to ensure gothic horror theme consistency..."
    },
    {
      "type": "command",
      "command": "npm run lint && npm run format"
    }
  ]
}
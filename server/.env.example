# SpookySites Environment Configuration
# Copy this file to .env and update with your own values

PORT=5001
NODE_ENV=development
JWT_SECRET=your_spooky_secret_key_here_change_this_in_production
JWT_EXPIRES_IN=7d

# MongoDB Connection
# Option 1: Use MongoDB Atlas (recommended)
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/spookysites?retryWrites=true&w=majority

# Option 2: Use local MongoDB (if installed locally)
# MONGODB_URI=mongodb://localhost:27017/spookysites

# Instructions:
# 1. Create a free MongoDB Atlas account at https://www.mongodb.com/atlas
# 2. Create a new cluster and database
# 3. Get your connection string and replace the MONGODB_URI above
# 4. Or use local MongoDB if you have it installed